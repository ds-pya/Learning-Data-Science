AppBar(
  automaticallyImplyLeading: false, // 필요시 뒤로가기 버튼 제거
  titleSpacing: 0,
  title: Row(
    mainAxisAlignment: MainAxisAlignment.spaceBetween,
    children: [
      // 왼쪽 Title + padding
      Padding(
        padding: const EdgeInsets.only(left: 12.0),
        child: Text(
          'Title',
          style: TextStyle(fontSize: 18, fontWeight: FontWeight.bold),
        ),
      ),

      // 중앙 날짜 선택
      Row(
        children: [
          IconButton(
            icon: Icon(Icons.chevron_left),
            onPressed: () {
              // 하루 이전 날짜
            },
          ),
          GestureDetector(
            onTap: () {
              // date picker 열기
            },
            child: Text(
              '25.07.31',
              style: TextStyle(fontSize: 16, fontWeight: FontWeight.w500),
            ),
          ),
          IconButton(
            icon: Icon(Icons.chevron_right),
            onPressed: () {
              // 하루 다음 날짜
            },
          ),
        ],
      ),

      // 오른쪽 설정 버튼
      IconButton(
        icon: Icon(Icons.settings),
        onPressed: () {
          // 설정 동작
        },
      ),
    ],
  ),
)